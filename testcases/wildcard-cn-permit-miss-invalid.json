{
  "name": "wildcard-cn-permit-miss-invalid",
  "expected": "invalid",
  "description": "A leaf with no SAN and a wildcard CN whose expansions do not fall inside any permitted DNS subtree must be rejected.",
  "references": [
    "RFC 5280 4.2.1.10",
    "RFC 6125",
    "Botan name_constraint_wildcard_cn/permit_miss"
  ],
  "notes": [
    "Falling back to CN for the DNS name constraint check requires the wildcard expansion to be inside the permitted subtree."
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "dns-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=Wildcard CN Permit Miss CA",
      "name_constraints": {
        "permitted": {
          "dns": [".attacker.com"]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "dns-ca",
      "subject": "C=US,O=NC Tests,CN=*.victim.com"
    }
  ]
}
