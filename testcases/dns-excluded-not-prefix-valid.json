{
  "name": "dns-excluded-not-prefix-valid",
  "expected": "valid",
  "description": "DNS exclusion is label based: excluding example.com must not reject testexample.com, which shares a suffix but not a label boundary.",
  "references": [
    "RFC 5280 4.2.1.10",
    "RFC 5280 7.2",
    "Go name_constraints_test \"excluded dns constraint is not a prefix match\""
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "dns-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=DNS Excluded Prefix CA",
      "name_constraints": {
        "excluded": {
          "dns": ["example.com"]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "dns-ca",
      "subject": "",
      "san": {
        "dns": ["testexample.com"]
      }
    }
  ]
}
