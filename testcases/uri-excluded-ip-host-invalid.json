{
  "name": "uri-excluded-ip-host-invalid",
  "expected": "invalid",
  "description": "A URI SAN whose host is an IP literal must be rejected when URI excluded constraints apply: a DNS-form URI constraint cannot match an IP host, so the SAN cannot be evaluated against it.",
  "references": [
    "RFC 5280 4.2.1.10",
    "Botan name_constraint_uri/excluded_ip_literal"
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "uri-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=URI Excluded IP Literal CA",
      "name_constraints": {
        "excluded": {
          "uri": [".example.com"]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "uri-ca",
      "subject": "",
      "san": {
        "uri": ["https://192.0.2.1/path"]
      }
    }
  ]
}
