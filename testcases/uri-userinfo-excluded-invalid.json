{
  "name": "uri-userinfo-excluded-invalid",
  "expected": "invalid",
  "description": "URI name-constraint processing must ignore the userinfo component and compare the authority host - even when the constraint is excluded. A leaf URI SAN with a 'user:pass@example.com' authority must be rejected by an exclude on example.com. A validator that compares the raw URI string in the excluded path (instead of the parsed host) would let the userinfo prefix bypass the exclude.",
  "references": [
    "RFC 5280 4.2.1.10",
    "https://github.com/openssl/openssl/commit/e599893a9f"
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "uri-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=URI Userinfo Exclude CA",
      "name_constraints": {
        "excluded": {
          "uri": [
            "example.com"
          ]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "uri-ca",
      "subject": "",
      "san": {
        "uri": [
          "https://user:pass@example.com/path"
        ]
      }
    }
  ]
}
