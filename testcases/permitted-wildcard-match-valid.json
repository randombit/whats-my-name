{
  "name": "permitted-wildcard-match-valid",
  "expected": "valid",
  "description": "A bare-host permittedSubtree (example.com) allows exact-host or single-label-deeper hosts; a wildcard SAN *.example.com can expand to example.com or host.example.com, both of which are allowed.",
  "references": [
    "RFC 5280 4.2.1.10",
    "RFC 6125"
  ],
  "notes": [
    "RFC 5280 does not define wildcard SAN semantics, this is codifying current implementation consensus."
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "dns-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=Wildcard Apex Mismatch CA",
      "name_constraints": {
        "permitted": {
          "dns": [
            "example.com"
          ]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "dns-ca",
      "subject": "C=US,O=NC Tests,CN=Wildcard Apex Leaf",
      "san": {
        "dns": [
          "*.example.com"
        ]
      }
    }
  ]
}
