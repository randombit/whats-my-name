{
  "name": "wildcard-cn-permit-match-valid",
  "expected": "valid",
  "description": "When a leaf has no SAN, DNS name constraints fall back to checking the CN. A wildcard CN whose expansions all lie inside a permitted DNS subtree must be accepted.",
  "references": [
    "RFC 5280 4.2.1.10",
    "RFC 6125",
    "Botan name_constraint_wildcard_cn/permit_match"
  ],
  "notes": [
    "Wildcard CN handling is not specified by RFC 5280 but is exercised by Botan, OpenSSL, and other validators."
  ],
  "certs": [
    {
      "id": "root",
      "ca": true,
      "self_signed": true,
      "subject": "C=US,O=NC Tests,CN=NC Test Root"
    },
    {
      "id": "dns-ca",
      "ca": true,
      "issuer": "root",
      "subject": "C=US,O=NC Tests,CN=Wildcard CN Permit CA",
      "name_constraints": {
        "permitted": {
          "dns": [".victim.com"]
        }
      }
    },
    {
      "id": "leaf",
      "issuer": "dns-ca",
      "subject": "C=US,O=NC Tests,CN=*.victim.com"
    }
  ]
}
